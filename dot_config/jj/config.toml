#:schema https://jj-vcs.github.io/jj/latest/config-schema.json

[user]
email = "felipeagc2@gmail.com"
name = "Felipe A. Costa"

[ui]
diff-editor = ["nvim", "-c", "DiffEditor $left $right $output"]
diff-formatter = ["delta"]
# diff-formatter = ["difft", "--color=always", "--width=160", "$left", "$right"]
# diff-formatter = ["delta", "-s", "--width=140", "$left", "$right"]
# diff-formatter = ["difft", "--color=always", "--width=160", "$left", "$right"]

# diff-format = "git"
# diff-formatter = "jj-delta-unified"
# pager = "delta"
#
# [merge-tools.jj-delta-unified]
# program = "bash"
# diff-args = ["-c", "git diff --no-index -U99999 -- $left $right | delta --width $width || true"]

# [merge-tools.delta]
# diff-args = [
#   "--width=$width",
#   "$left",
#   "$right",
# ]
# diff-expected-exit-codes = [0, 1]
